{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelButtonClicked = id; downloadButtonClicked = id; }; 
            CLASS = "ch.cyberduck.ui.cocoa.CDDownloadController"; 
            LANGUAGE = Java; 
            OUTLETS = {urlField = NSTextField; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}