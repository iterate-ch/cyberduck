{
    IBClasses = (
        {
            ACTIONS = {
                reloadButtonClicked = id; 
                removeButtonClicked = id; 
                resumeButtonClicked = id; 
                revealButtonClicked = id; 
                stopButtonClicked = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = "ch.cyberduck.core.CDQueueController"; 
            LANGUAGE = Java; 
            OUTLETS = {queueTable = NSTableView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = "ch.cyberduck.ui.cocoa.CDQueueTable"; 
            LANGUAGE = Java; 
            SUPERCLASS = NSTableView; 
        }
    ); 
    IBVersion = 1; 
}