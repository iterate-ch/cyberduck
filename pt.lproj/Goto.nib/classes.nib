{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelButtonClicked = id; goButtonClicked = id; }; 
            CLASS = "ch.cyberduck.ui.cocoa.CDGotoController"; 
            LANGUAGE = Java; 
            OUTLETS = {folderField = NSTextField; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}