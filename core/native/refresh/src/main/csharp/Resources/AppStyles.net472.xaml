<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:system="clr-namespace:System;assembly=System.Runtime">
    <FontFamily x:Key="SymbolThemeFontFamily">Segoe Fluent Icons, Segoe MDL2 Assets</FontFamily>

    <sys:Double x:Key="CaptionTextBlockFontSize">12</sys:Double>
    <sys:Double x:Key="BodyTextBlockFontSize">14</sys:Double>
    <sys:Double x:Key="SubtitleTextBlockFontSize">20</sys:Double>
    <sys:Double x:Key="TitleTextBlockFontSize">28</sys:Double>
    <sys:Double x:Key="TitleLargeTextBlockFontSize">40</sys:Double>
    <sys:Double x:Key="DisplayTextBlockFontSize">68</sys:Double>
    <system:Double x:Key="TextControlThemeMinHeight">24</system:Double>
    <system:Double x:Key="TextControlThemeMinWidth">0</system:Double>
    <Thickness x:Key="TextControlThemePadding">10,5,6,6</Thickness>
    <Style x:Key="BaseTextBlockStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{StaticResource BodyTextBlockFontSize}" />
        <Setter Property="FontWeight"
                Value="SemiBold" />
        <Setter Property="TextTrimming"
                Value="CharacterEllipsis" />
        <Setter Property="TextWrapping"
                Value="Wrap" />
        <Setter Property="LineStackingStrategy"
                Value="MaxHeight" />
    </Style>
    <Style x:Key="CaptionTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontSize"
                Value="{StaticResource CaptionTextBlockFontSize}" />
        <Setter Property="FontWeight"
                Value="Normal" />
    </Style>
    <Style x:Key="BodyTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontWeight"
                Value="Normal" />
    </Style>
    <Style x:Key="BodyStrongTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}" />
    <Style x:Key="SubtitleTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontSize"
                Value="{StaticResource SubtitleTextBlockFontSize}" />
    </Style>
    <Style x:Key="TitleTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontSize"
                Value="{StaticResource TitleTextBlockFontSize}" />
    </Style>
    <Style x:Key="TitleLargeTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontSize"
                Value="{StaticResource TitleLargeTextBlockFontSize}" />
    </Style>
    <Style x:Key="DisplayTextBlockStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="FontSize"
                Value="{StaticResource DisplayTextBlockFontSize}" />
    </Style>
    <Style x:Key="DefaultTextBoxStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="HorizontalContentAlignment"
                Value="Left" />
        <Setter Property="VerticalContentAlignment"
                Value="Top" />
        <Setter Property="MinHeight"
                Value="{DynamicResource TextControlThemeMinHeight}" />
        <Setter Property="MinWidth"
                Value="{DynamicResource TextControlThemeMinWidth}" />
        <Setter Property="Padding"
                Value="{DynamicResource TextControlThemePadding}" />
    </Style>
    <Style TargetType="TextBox"
           BasedOn="{StaticResource DefaultTextBoxStyle}" />

    <Thickness x:Key="ButtonPadding">11,5,11,6</Thickness>
    <Style x:Key="DefaultButtonStyle"
           TargetType="ButtonBase">
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <Setter Property="Padding"
                Value="{StaticResource ButtonPadding}" />
    </Style>
    <Style TargetType="ButtonBase"
           BasedOn="{StaticResource DefaultButtonStyle}" />
    <Style TargetType="Button"
           BasedOn="{StaticResource DefaultButtonStyle}" />

    <Style x:Key="DialogMenuStyle"
           TargetType="Menu">
        <Setter Property="TextElement.FontSize"
                Value="{StaticResource BodyTextBlockFontSize}" />
        <Setter Property="Padding"
                Value="{StaticResource UISpacing}" />
    </Style>
    <Style TargetType="MenuItem">
        <Setter Property="Padding"
                Value="{StaticResource ButtonPadding}" />
    </Style>

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="AppStyles.Base.xaml" />
    </ResourceDictionary.MergedDictionaries>
</ResourceDictionary>
