{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {closeSheet = id; }; 
            CLASS = "ch.cyberduck.ui.cocoa.CDFileController"; 
            LANGUAGE = Java; 
            OUTLETS = {
                editButton = NSButton; 
                filenameField = NSTextField; 
                iconView = NSImageView; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}