{
    IBClasses = (
        {CLASS = CDErrorView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {close = id; }; 
            CLASS = "ch.cyberduck.ui.cocoa.CDErrorController"; 
            LANGUAGE = Java; 
            OUTLETS = {alertIcon = NSButton; errorField = NSTextField; view = NSView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}