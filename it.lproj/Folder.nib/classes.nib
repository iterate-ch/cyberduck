{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancelButtonClicked = id; createButtonClicked = id; }; 
            CLASS = "ch.cyberduck.ui.cocoa.CDFolderController"; 
            LANGUAGE = Java; 
            OUTLETS = {folderField = NSTextField; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}