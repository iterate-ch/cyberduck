{
    IBClasses = (
        {CLASS = CDListView; LANGUAGE = ObjC; SUPERCLASS = NSTableView; }, 
        {CLASS = CDOutlineView; LANGUAGE = ObjC; SUPERCLASS = NSOutlineView; }, 
        {
            ACTIONS = {closeSheet = id; }; 
            CLASS = "ch.cyberduck.ui.cocoa.CDTransferPrompt"; 
            LANGUAGE = Java; 
            OUTLETS = {
                actionPopup = NSPopUpButton; 
                browserView = NSTableView; 
                localModificationField = NSTextField; 
                localSizeField = NSTextField; 
                localURLField = NSTextField; 
                remoteModificationField = NSTextField; 
                remoteSizeField = NSTextField; 
                remoteURLField = NSTextField; 
                statusIndicator = NSProgressIndicator; 
                statusLabel = NSTextField; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}