{
    IBClasses =     (
        {
    ACTIONS =     {
        closeSheet = id;
    };
    CLASS = CDGotoController;
    LANGUAGE = ObjC;
    OUTLETS =     {
        folderCombobox = NSComboBox;
        iconView = NSImageView;
        window = NSWindow;
    };
    SUPERCLASS = NSObject;
},
        {
    CLASS = FirstResponder;
    LANGUAGE = ObjC;
    SUPERCLASS = NSObject;
}
    );
    IBVersion = 1;
}